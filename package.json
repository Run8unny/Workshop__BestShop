{
  "name": "workshop__bestshop",
  "version": "2.0.0",
  "description": "This is the workshop for HTML and CSS bootcamp.",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel --no-cache",
    "build": "parcel build",
    "deploy": "node deploy.js"
  },
  "dependencies": {
    "execa": "^8.0.1",
    "parcel": "^2.11.0",
    "sass": "^1.71.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0"
  }
}
